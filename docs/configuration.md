# Configuration

The active configuration guide lives at [usage/configuration-guide.md](usage/configuration-guide.md).

Key detection is optional. Use the native `auto`, `portable_basic`, or `disabled` backends through Noqlen Forge Core configuration; unavailable optional backends should skip cleanly rather than blocking metadata workflows.
